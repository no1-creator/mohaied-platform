# قاعدة البيانات (PostgreSQL)
DATABASE_URL="postgresql://postgres:password@localhost:5432/mohaied?schema=public"

# إعدادات السيرفر
PORT=4000
NODE_ENV=development

# المصادقة (JWT)
JWT_SECRET="change_this_to_a_long_random_secret"
JWT_EXPIRES_IN="7d"
